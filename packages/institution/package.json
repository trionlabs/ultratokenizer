{
  "name": "@ultratokenizer/institution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json && node scripts/build.mjs",
    "test": "npm run build && node --test dist/test/*.test.js"
  }
}
