{
  "name": "@ultratokenizer/issuance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json && node scripts/build.mjs",
    "test": "npm run build && node --test dist/test/*.test.js"
  }
}
